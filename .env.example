# URL endpoint sinkronisasi asinkron (dipakai saat perangkat online).
# Fallback kosong = mode luring penuh.
# Contoh server dev lokal (lihat server/):
# SYNC_API_URL=http://192.168.1.10:3000/sync
SYNC_API_URL=
